# Erlang version
erlang_version=21.3

# Elixir version
elixir_version=1.10

# Always rebuild from scratch on every deploy?
always_rebuild=false

# Export heroku config vars
config_vars_to_export=(DATABASE_URL MIX_ENV PORT)

# A command to run right after compiling the app
post_compile="pwd"